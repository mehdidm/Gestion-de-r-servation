{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}
{% block stylesheets %}
<link rel="stylesheet" href="/css/service.css">


{% endblock %}
{% block body %}
    <h1>Client:{{app.user.nom}}</h1>

    <table class="table table-info t">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Nom_cli</th>
                <td>{{ client.nomCli }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ client.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ client.adresse }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ client.email }}</td>
            </tr>
        </tbody>
        <a href="{{ path('client_edit', {'id': client.id}) }}"class="btn btn-info">edit</a>
        {{ include('client/_delete_form.html.twig') }}
    </table>

    

    

    
{% endblock %}
